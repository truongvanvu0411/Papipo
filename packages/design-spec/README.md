# Papipo Design Spec

These tokens are extracted from the prototype in `legacy/prototype-web`.

- Keep the warm claymorphism look and the `Nunito` typography
- Reuse the same semantic colors in Flutter and the admin dashboard
- Treat the original React prototype as the visual source of truth while the new platform is being implemented
